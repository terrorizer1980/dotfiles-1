#!/bin/zsh -f

source ~/.zshrc

s=`spotify-cli now-playing`
a=("${(f)s}")

x=$(echo ${a[1]} | grep -Eo '"(.*)"' | sed 's/"//g')
y=$(echo ${a[2]} | grep -Eo '"(.*)"' | sed 's/"//g')

echo "♫ $x - $y"
