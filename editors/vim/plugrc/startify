" Startify — The fancy start screen for Vim

Plug 'mhinz/vim-startify'

" Customize the Startify header
let g:startify_custom_header =
      \ map(split(system('cat ~/.vim/banner'), '\n'), '"   ". v:val')


" vim:ft=vim
